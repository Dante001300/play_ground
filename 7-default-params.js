const greeter = (name = 'user') => { // modify the default value from undefined to user by adding = 'user' after name
}
greeter()  // hello user will print

greeter('dante')  // hello dante will print